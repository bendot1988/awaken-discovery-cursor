# Awaken Discovery — TODO

> ⚠️  **This file is no longer the source of truth.**
>
> All current outstanding work (workflow.design feedback + pre-launch audit) lives in
> **[golive.md](./golive.md)** at the repo root.
>
> That file is non-indexable (excluded from the production build and listed in
> `public/robots.txt`) and is kept up to date as items are closed.

## Why this file still exists

Kept as a redirect so historical links from commits/issues/PRs don't 404 onto a deleted file. The previous content has been merged into `golive.md` (any items not yet resolved are now tracked there under Parte B / Parte C).

## When it's safe to delete

When you're confident no external references (Linear / GitHub / docs) still link to `TODO.md`. Until then, leave this stub in place.
