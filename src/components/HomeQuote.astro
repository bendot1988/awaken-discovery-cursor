<section class="section section-quote">
	<div class="container quote-stack">
		<blockquote>
			<p>
				"Travelling a journey of confusion, pain and suffering, awakens the deepest desire to
				recognise oneself."
			</p>
		</blockquote>
		<p>
			I offer a warm, supportive and non-judgemental space where we can explore whatever you need
			at your pace.
		</p>
	</div>
</section>

<style>
	.section-quote {
		background: var(--color-light-gray);
	}

	.quote-stack {
		display: grid;
		gap: 1rem;
	}

	blockquote {
		margin: 0;
		padding-left: 1rem;
		border-left: 4px solid var(--color-dark-slate-gray);
		font-family: var(--font-serif);
		font-size: clamp(1.5rem, 3vw, 2.2rem);
		line-height: 1.35;
	}

	blockquote p {
		margin: 0;
	}
</style>
