<section class="hero section">
	<div class="container hero-grid">
		<div class="hero-copy">
			<p class="eyebrow">Free 1-2-1</p>
			<h1>Counselling and Psychotherapy practitioner</h1>
			<p class="intro">
				Hello, my name is Ally, and my private practice Awaken Discovery welcomes you. I work
				with individuals and couples online, by telephone and face-to-face from Market
				Weighton, East Yorkshire.
			</p>
			<div class="cta-row">
				<a class="cta cta-solid" href="#contact">Book your taster session</a>
				<a class="cta cta-outline" href="#contact">Free question and answers meeting</a>
			</div>
		</div>
		<div class="hero-media">
			<img src="/assets/images/hero-ally.jpeg" alt="Therapy practitioner portrait" />
		</div>
	</div>
</section>

<style>
	.hero-grid {
		display: grid;
		gap: 2rem;
		align-items: center;
		grid-template-columns: 1.15fr 0.85fr;
	}

	.hero-copy h1 {
		margin: 0.5rem 0 1rem;
		font-family: var(--font-serif);
		font-size: clamp(1.95rem, 4vw, 3.2rem);
		font-weight: 500;
		line-height: 1.2;
		text-transform: capitalize;
	}

	.eyebrow {
		margin: 0;
		color: var(--color-dark-slate-gray);
		font-weight: 500;
		letter-spacing: 0.04em;
		text-transform: uppercase;
	}

	.intro {
		max-width: 64ch;
		margin: 0;
		font-size: 1.05rem;
		line-height: 1.85;
	}

	.hero-media img {
		width: 100%;
		border-radius: 1rem;
		object-fit: cover;
		box-shadow: 0 12px 28px rgb(29 35 39 / 16%);
	}

	.cta-row {
		display: flex;
		flex-wrap: wrap;
		gap: 0.75rem;
		margin-top: 1.65rem;
	}

	@media (max-width: 1024px) {
		.hero-grid {
			grid-template-columns: 1fr;
		}
	}
</style>
