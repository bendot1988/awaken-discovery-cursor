<section class="hero section">
	<div class="container hero-shell">
		<p class="eyebrow">Transition Expert Support</p>
		<h1>Expert Counselling for Life Transitions &amp; Career Burnout</h1>
		<p class="subheadline">
			Helping long-term professionals navigate the anxiety of midlife, rediscover their identity,
			and find the courage for their "Great Escape."
		</p>
		<p class="body-copy">
			You've spent decades building a career, but now you feel lost in the life you created.
			Whether you need professional support for burnout or a soulful guide to help you transition
			into your next chapter, I provide the clinical expertise and real-world experience to help
			you bridge the gap between where you are and where you're meant to be.
		</p>
		<div class="cta-row">
			<a class="cta hero-primary" href="#contact">Book a Discovery Call</a>
			<a class="cta hero-ghost" href="#contact">Get the "Career Transition" Roadmap</a>
		</div>
	</div>
</section>

<style>
	.hero {
		--earth-tone: #b59a7a;
		--earth-tone-deep: #9d8467;
		padding-block: clamp(6rem, 14vh, 9.5rem);
		min-height: 74vh;
		background-image:
			linear-gradient(90deg, rgb(255 255 255 / 95%) 0%, rgb(255 255 255 / 92%) 52%, rgb(255 255 255 / 72%) 100%),
			url("/assets/images/hero-ally.jpeg");
		background-size: cover;
		background-position: center right;
		display: flex;
		align-items: center;
	}

	.hero-shell {
		max-width: 760px;
	}

	h1 {
		margin: 0.7rem 0 1rem;
		font-family: var(--font-serif);
		font-size: clamp(2rem, 5.2vw, 3.8rem);
		font-weight: 500;
		line-height: 1.12;
		letter-spacing: 0.01em;
		text-wrap: balance;
	}

	.eyebrow {
		margin: 0;
		color: var(--earth-tone-deep);
		font-weight: 500;
		letter-spacing: 0.08em;
		font-size: 0.82rem;
		text-transform: uppercase;
	}

	.subheadline {
		margin: 0;
		font-family: var(--font-body);
		font-size: clamp(1.05rem, 2.4vw, 1.35rem);
		font-weight: 400;
		line-height: 1.65;
		max-width: 62ch;
	}

	.body-copy {
		max-width: 64ch;
		margin: 1.15rem 0 0;
		font-size: 1.02rem;
		line-height: 1.95;
		color: rgb(29 35 39 / 90%);
	}

	.cta-row {
		display: flex;
		flex-wrap: wrap;
		gap: 0.85rem;
		margin-top: 2rem;
	}

	.hero-primary {
		background: var(--earth-tone);
		color: var(--color-white);
		border: 1px solid var(--earth-tone);
		transition: background-color 0.2s ease, border-color 0.2s ease;
	}

	.hero-primary:hover,
	.hero-primary:focus-visible {
		background: var(--earth-tone-deep);
		border-color: var(--earth-tone-deep);
	}

	.hero-ghost {
		background: transparent;
		border: 1px solid var(--color-dark-gray);
		color: var(--color-dark-gray);
		transition: background-color 0.2s ease, color 0.2s ease;
	}

	.hero-ghost:hover,
	.hero-ghost:focus-visible {
		background: rgb(255 255 255 / 70%);
	}

	@media (max-width: 1024px) {
		.hero {
			min-height: auto;
			background-position: center;
		}
	}
</style>
